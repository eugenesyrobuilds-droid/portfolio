import DriverContent from "./DriverContent";

export const metadata = {
  title: "Pre-Trip Driver Medical Checkups — Eugene Syromiatnikov",
  robots: { index: false, follow: false },
};

export default function DriverCaseStudyPage() {
  return <DriverContent />;
}
