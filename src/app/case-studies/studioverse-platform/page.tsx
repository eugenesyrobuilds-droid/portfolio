import StudioverseContent from "./StudioverseContent";

export const metadata = {
  title: "Multi-Tenant Platform for AI Video Production Agency — Eugene Syromiatnikov",
  robots: { index: false, follow: false },
};

export default function StudioversePlatformCaseStudyPage() {
  return <StudioverseContent />;
}
